
## 会议室 253

### 关键问题分析

#### 问题本质 

需要判断同一时刻最多有多少个会议同时进行，从而确定所需会议室数量

#### 核心矛盾

会议是开始时间和结束时间会相互影响：

- 当新会议开始的时候，如果之前的会议结束了，会议室可以复用
- 否则需要新增会议室

### 思考步骤

#### 暴力法

- 遍历每个时间点（如分钟级粒度），统计当前有多少会议在进行
- 问题：时间复杂度极高（O(m, n)，m 为时间范围，n为会议数）
- 启示：无需关注具体时间点，只需要关注会议的开始和结束两个关键事件

```python
def minMeetingRooms(intervals):
	if not intervals:
		return 0

	# 找到所有会议的最早开始时间和最晚结束时间
	min_start = min(i[0] for i in intervals)
	max_end = max(i[1] for i in intervals)

	max_rooms = 0
	# 遍历每个时间点
	for t in range(min_start, max_end + 1):
		count = 0
		# 统计当前时间点有多少会议在进行
		for start, end in intervals:
			if start <= t < end:
				count += 1
		# 更新最大会议需求
		max_rooms = max(max_rooms, count)
	return max_rooms

# 示例测试
intervals = [[0, 30], [5, 10], [15, 20]]
print(minMeetingRooms(intervals)) # 输出 2
```

## 多数元素 169

```python
def majorityElement(nums):
	count_map = {}
	# 统计每个元素的出现次数
	for num in nums:
		count_map[num] = count_map.get(num, 0) + 1

	# 找出次数超过一半的元素
	majority_count = len(nums) // 2
	for num, count in count_map.items():
		if count > majority_count:
			return num

	return None

nums = [3, 2, 3]
print(majorityElement(nums)) # 输出：3
```