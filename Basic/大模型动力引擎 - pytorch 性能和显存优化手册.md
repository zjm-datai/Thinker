
# 1 欢迎来到这场大模型竞赛

## 1.1 模型规模带来的挑战

## 1.2 数据规模带来的挑战

## 1.3 模型规模和数据规模的应对方法

# 2 深度学习必备的硬件知识

## 2.1 CPU 和内存

### 2.1.1 内存

### 2.1.2 CPU

CPU 频率衡量的是单个 CPU 核心的指令执行效率，然而现代 CPU 往往采用多核心设计，这是为了提高 CPU 并行处理任务的速度。CPU 核心数量对于深度学习的重要程度不亚于 CPU 频率，**这是因为更多核心数量能够显著提高训练过程中数据加载、清洗和预处理等任务的速度。**

## 2.2 硬盘

固态硬盘的读写速度要远超机械硬盘，而机械硬盘则只在成本上具有优势。

## 2.3 GPU

### 2.3.1 CPU 的局限性

深度学习领域同样采纳这种分工方法，核心的计算任务通常被封装成算子并在 GPU 上执行，而较为复杂的逻辑调度任务则由 CPU 处理。这样的做法有效地利用了各类硬件的专长，优化了整体计算效率。

### 2.3.2 GPU 的硬件结构

### 2.3.3 GPU 编程模型及其硬件对应

我们知道，Python、C++ 等程序能够在计算机上运行，是因为它们通过编译器转换成硬件能识别的指令，从而在 CPU 上执行。基于这一逻辑，如果存在一个能将 C++ 或 Python 编译成 GPU 指令的编译器，我们就可以直接在 GPU 上运行这些代码。然而，遗憾的是，NVIDIA 没有提供这种直接的编译器。相反，NVIDIA 开发了一套专门的 GPU 编程模型，称为 CUDA 语言，来支持在 GPU 上进行编程。

### 2.3.4 GPU的关键性能指标

## 2.4 分布式系统

分布式训练本质上是让多张 GPU 卡共同参与训练过程，从而达到加速或者扩大可用显存的目的，但是代价是必须引进一种新的数据传输类型 -- GPU 多卡间的数据通信。至于 GPU 多卡之间具体传输什么数据内容，我们后面第八章再议。这里只需要了解多张 GPU 卡间通信的数据量往往很大，且通信频率不低。

### 2.4.1 单机多卡的通信

### 2.4.2 多机多卡的通信

### 2.4.3 分布式系统的数据存储

基于网络的存储方案在分布式系统中更受欢迎。通常也分为两类，NFS 和基于云服务的存储方案。

# 3 深度学习必备的 PyTorch

## 3.2 PyTorch 中的算子

## 3.3 PyTorch 的动态图机制

## 3.4 PyTorch 的自动微分系统

### 3.4.1 什么是自动微分

## 3.5 PyTorch 的异步执行机制

那么 CPU 是怎么将算子任务放在 GPU 上面去执行的呢？