### 自己建立结构体类型

### 定义结构体类型变量

#### 先声明结构体的类型，再定义该类型的变量

#### 在声明类型的同时定义变量

```c
struct Student {
	int num;
	char name[20];
	char sex;
	int age;
	float score;
	char addr[30];
} student1, student2;
```

#### 不指定类型名而直接定义结构体变量

### 结构体变量的初始化和引用

在定义结构体变量的时候，可以对他进行初始化，也就是赋予初始值。然后可以引用这个变量，例如输出它的成员的值。

