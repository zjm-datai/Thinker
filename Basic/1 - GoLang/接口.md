
### 什么是 Go 语言中的接口？

Go 语言中的接口是一种抽象类型，它定义了一组方法签名，但是不包含方法的实现。接口是一种契约，实现了这些方法的类型就被认为实现了这个接口，无需显示声明。例如：

```go
type Reader interface {
	Read(p []byte) (n int, err error)
}
```

任何定义了 `Read` 方法的类型都隐式实现了 `Reader` 接口。

