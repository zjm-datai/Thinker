
```python
from fastapi import FastAPI, Depends
from datetime import datetime
import uvicorn

app = FastAPI()

class MyService:
    def __init__(self):
        # 打印创建时间和内存地址，方便区分实例
        print(f"[{datetime.now()}] Service created! id={id(self)}")

def get_service():
    return MyService()

@app.get("/one")
def one(s1: MyService = Depends(get_service)):
    return {"instance_id": id(s1)}

@app.get("/two")
def two(
    s1: MyService = Depends(get_service),
    s2: MyService = Depends(get_service)  # 同一个请求注入两次
):
    return {
        "s1_id": id(s1),
        "s2_id": id(s2),
        "same_instance": s1 is s2
    }

if __name__ == "__main__":
    uvicorn.run(app, host="127.0.0.1", port=8084)

```

```
curl http://127.0.0.1:8084/two
{"s1_id":140516360405936,"s2_id":140516360405936,"same_instance":true}
```

```
(kn-audio) (base) root@11110000:/data/services/kn_audio# curl http://127.0.0.1:8084/one
{"instance_id":140516360406752}

(kn-audio) (base) root@11110000:/data/services/kn_audio# curl http://127.0.0.1:8084/one
{"instance_id":140516360406032}
```

### 单例模式

```python
from fastapi import FastAPI, Depends
import uvicorn

app = FastAPI()

class SingletonService:
    _instance = None

    def __new__(cls, *args, **kwargs):
        if not cls._instance:
            print("SingletonService created!")
            cls._instance = super().__new__(cls)
        return cls._instance

def get_service():
    return SingletonService()

@app.get("/")
def index(service: SingletonService = Depends(get_service)):
    return {"id": id(service)}

if __name__ == "__main__":
    uvicorn.run(app, host="127.0.0.1", port=8084)
```

```
(kn-audio) (base) root@11110000:/data/services/kn_audio# curl http://127.0.0.1:8084/
{"id":140378829964064}

(kn-audio) (base) root@11110000:/data/services/kn_audio# curl http://127.0.0.1:8084/
{"id":140378829964064}

(kn-audio) (base) root@11110000:/data/services/kn_audio# curl http://127.0.0.1:8084/
{"id":140378829964064}

(kn-audio) (base) root@11110000:/data/services/kn_audio# curl http://127.0.0.1:8084/
{"id":140378829964064}
```


