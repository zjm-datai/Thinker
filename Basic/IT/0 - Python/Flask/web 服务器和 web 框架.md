
## Flask 和 Web 服务器的基础配合机制

Flask 自身的 Web 服务器（开发服务器）的工作原理是什么？为什么不适合生产环境？

生产环境中，Flask 应用通常和哪些 Web 服务器进行配合？Gunicorn uWSGI Nginx 等服务器，它们各自的角色是什么？

Flask 应用和 Web 服务器之间通过什么样的协议/接口进行配合？如 WSGI 协议的具体作用？

当用户请求到达的时候，从客户端到 Flask 视图函数的完整流程是什么？包含 DNS Nginx Web 服务器，Flask 应用的层级交互

## 进程和线程模型相关

Flask 开发服务器的默认进程，线程配置是怎么样的？单进程还是多进程？

生产环境中，Web 服务器（如 Gunicorn）通常是如何配置进程数和线程数量的？这些配置受到哪些因素的影响？如 GPU 核心数，内存，请求类型等。

进程和线程在处理 Flask 请求的时候的资源分配方式有什么不同？如内存占用，上下文切换，切换开销等。

Flask 应用中的全局变量（如 g 对象，session）在多进程/多线程环境下是如何保证线程安全/进程安全的？

当 Flask 应用处理一个长耗时请求（比如数据库查询）的时候，会对同一个进程/线程中的其他请求产生什么影响？

## 横向扩展的核心问题

什么是 “横向扩展”？与 “纵向扩展” 相比，它在 Flask 应用中的优势是什么？

实现 Flask 横向扩展的主要方案有哪些？（如多实例部署、负载均衡、容器化等）

多实例部署时，Flask 应用的会话（Session）如何在多个实例间共享？（如 Redis 存储、数据库存储的方案）

无状态设计对 Flask 横向扩展的重要性是什么？哪些功能可能导致应用 “有状态”，需要特别处理？

负载均衡器（如 Nginx、HAProxy）在 Flask 横向扩展中扮演什么角色？常见的负载均衡策略有哪些？


## 进阶与实践问题

当 Flask 应用部署在多个服务器节点时，如何处理静态资源（如 CSS、JS、图片）的访问？

分布式环境下，Flask 应用的日志如何收集和集中管理？

数据库连接在多实例 Flask 应用中如何管理？（如连接池配置、避免连接泄露）

如何对横向扩展后的 Flask 集群进行性能监控和压力测试？（关注哪些指标？）

容器化（Docker）和编排工具（K8s）如何简化 Flask 应用的横向扩展流程？





































