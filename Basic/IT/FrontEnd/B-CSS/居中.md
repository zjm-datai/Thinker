### TODO:

我的页面在打包部署之后，运行在 app 的 web-view 中出现了 输入框和聊天区域整个左移动，感觉像是他们所处的区域宽度 x 2倍了一样的感觉，请你告诉我为什么。从我的样式进行分析： 

```vue
<!-- frontend/src/views/ChatPage.vue -->

<template>
    <div class="relative w-full h-screen overflow-hidden">
        <!-- 顶部区域 -->
        <div
          id="top-area"
          class="relative w-full h-[34.9vh] overflow-hidden flex flex-col transition-all duration-300"
        >
          <!-- 背景图 -->
          <img
            :src="topBackground"
            class="absolute inset-0 h-full w-full object-cover"
            alt="banner"
          />

          <!-- Logo + 标语 -->
          <div
            id="welcome"
            v-show="!isKeyboardVisible"
            class="relative z-10 flex flex-col items-center gap-4 pt-[1.2vh] px-[4vw]"
          >
            <div
              class="flex items-center gap-[4vw] mx-auto pt-[3.6vh] px-[4vw]"
            >
              <h1
                class="text-gray-800 font-semibold text-center
                leading-snug text-[3.3vw] md:text-[1.8vw] xl:text-[1.6vw]"
              >
                为了让医生更全面地了解您的病情，以便为您提供更好的诊疗服务，
                <br />
                AI 助手将询问您相关问题，麻烦您用几分钟的时间如实回答以下问题，
                感谢您的配合！
              </h1>
            </div>
          </div>
        </div>


        <!-- 患者信息 -->
        <div
          id="patient-info"
          class="absolute z-20 flex items-center justify-evenly w-full text-sm font-semibold transition-all duration-300"
          :style="{
            top: isKeyboardVisible ? '10vh' : 'calc(20.4vh - 5vh)'
          }"
        >
          <span>姓名：{{ patient.name }}</span>
          <span>年龄：{{ patient.age }}</span>
          <span>就诊号：{{ patient.caseNo }}</span>
        </div>

        <!-- 主体 -->
        <div
            id="main-area"
            class="flex flex-col flex-grow pt-4 pb-24 absolute top-[20.4vh] w-full h-[79.6vh] bg-[#f4f7fd] rounded-t-[24px]"
        >
            <div class="flex-grow w-full overflow-hidden">
                <!-- 预留视频区域 -->
                <!-- <div id="video-person" class="w-1/3 p-4" /> -->

                <div class="overflow-y-auto h-full w-full" ref="chatAreaRef">
                  <!-- 聊天区域 -->
                  <div
                      id="chat-area"
                      class="flex flex-col flex-grow p-6 pb-[88px] overflow-hidden space-y-4 text-sm md:mx-[25vw]"
                  >
                      <div
                          v-for="(msg, idx) in messages"
                          :key="idx"
                          :class="msg.role === 'user' ? 'text-right' : 'text-left'"
                      >

                          <el-avatar
                              v-if="msg.role === 'assistant'"
                              :src="doctorAvatar"
                              size="default"
                              class="mr-2 shadow-lg"
                          />

                          <div
                              :class="[
                                  'inline-block px-4 py-3 rounded-lg whitespace-pre-wrap break-words max-w-[80%]',
                                  msg.role === 'user'
                                  ? 'bg-blue-500 text-white text-left'
                                  : 'text-gray-800',
                              ]"
                          >
                              {{ msg.content }}
                          </div>
                      </div>
                  </div>
                </div>

            </div>
        </div>

        <!-- 输入框 -->
        <div
          id="input-area"
          class="fixed bottom-[calc(env(safe-area-inset-bottom)+1rem)] left-1/2 transform -translate-x-1/2 w-full max-w-4xl px-4"
        >
          <ChatInput
            v-model="inputMessage"
            placeholder="请输入消息"
            @send="sendMessage"
            @attachment-click="handleAttachmentClick"
            @audio-click="handleAudioClick"
            @mic-press-start="startRecording"
            @mic-press-end="stopRecording"
            :is-recording="isRecording"
            :is-audioing="isAudioing"
          />
        </div>


        <!-- 录音提示 -->
        <transition name="fade">
          <div
            v-if="isRecording"
            class="fixed z-50 left-1/2 bottom-[120px] transform -translate-x-1/2
            bg-black bg-opacity-70 text-white px-6 py-4 rounded-xl flex flex-col items-center animate-pulse"
          >
            <!-- 使用 lucide 图标 -->
            <AudioWaveformIcon class="w-10 h-10 mb-2 animate-pulse" />
            <span class="text-sm">
              正在录音，松开发送
            </span>
          </div>
        </transition>

    </div>
</template>
```

