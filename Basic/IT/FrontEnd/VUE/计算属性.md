
## 基础示例

模板中的表达式虽然很方便，但是也只能用来做一些简单的操作。如果在模板中写太多逻辑，会让模板变得臃肿，难以维护。比如，我们有这样一个包含嵌套数组的对象：

```js
const author = reactive({
	name: 'John Doe',
	books: [
		'Vue2 - Advanced Guide',
		'Vue3 - Basic Guide'
	]
})
```

### 计算属性缓存 vs 方法


### 可读计算属性

