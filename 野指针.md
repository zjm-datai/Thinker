“野指针”（Wild Pointer）是 C/C++ 等底层语言中常见的内存错误概念，指的是 **指向无效内存区域的指针**。这种指针可能指向：

- 已经被释放（回收）的内存
- 未分配的内存区域
- 受系统保护的内存（如内核空间）

当程序试图通过野指针访问或修改内存时，会触发 **内存访问违规**，操作系统会直接终止程序（在 Unix/Linux 系统中表现为 “段错误” Segmentation Fault）。